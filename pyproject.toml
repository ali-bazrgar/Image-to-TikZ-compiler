[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "image-to-tikz-compiler"
version = "0.1.0"
description = "Image analysis pipeline that converts raster diagrams into an LLM-readable visual intermediate representation (VIR)."
requires-python = ">=3.10"
dependencies = [
  "opencv-python>=4.9",
  "numpy>=1.26",
  "Pillow>=10.0",
]

[project.optional-dependencies]
ocr = ["pytesseract>=0.3.13"]
llm = ["requests>=2.32"]
all = ["pytesseract>=0.3.13", "requests>=2.32"]

[project.scripts]
image-to-vir = "image_to_tikz.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
